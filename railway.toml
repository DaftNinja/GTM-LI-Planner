generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

# Note: This app uses raw PostgreSQL with pg library
# For Prisma alternative, use: prisma migrate dev --name init
